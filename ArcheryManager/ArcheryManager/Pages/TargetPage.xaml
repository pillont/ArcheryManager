<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:targ="clr-namespace:ArcheryManager.CustomControls.Targets"
             xmlns:ctl="clr-namespace:ArcheryManager.CustomControls"
             xmlns:controls="clr-namespace:XFShapeView;assembly=XFShapeView"
             xmlns:shape="clr-namespace:XFShapeView;assembly=XFShapeView"
             x:Class="ArcheryManager.Pages.TargetPage">

    <StackLayout>
        <!-- TODO : target update-->
        <targ:EnglishTarget x:Name="customTarget"  HeightRequest="1200" WidthRequest="1200" />
        <Button Text="remove last" Clicked="Button_RemoveLast" />
        <Button Text="remove all"  Clicked="Button_RemoveAll" />

        <ScrollView x:Name="scrollArrows" MinimumHeightRequest="60" HeightRequest="60">
            <ctl:ArrowCustomGrid x:Name="scoreList"  Grid.Column="0" MinWidth="49" />
        </ScrollView>
    </StackLayout>
</ContentPage>
