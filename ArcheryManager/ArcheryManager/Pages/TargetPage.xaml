<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ctl="clr-namespace:ArcheryManager.CustomControls"
             xmlns:controls="clr-namespace:XFShapeView;assembly=XFShapeView"
             xmlns:shape="clr-namespace:XFShapeView;assembly=XFShapeView"
             x:Class="ArcheryManager.Pages.TargetPage">

    <StackLayout>
        <ctl:TotalCounter x:Name="totalCounter" />
        <ctl:Target x:Name="customTarget"  HeightRequest="1200" WidthRequest="1200" />

        <StackLayout Orientation="Horizontal">
            <Button AutomationId="unSelectButton" x:Name="unSelectButton" Text="Unselect" />
            <Button AutomationId="removeSelectionButton" x:Name="removeSelectionButton" Text="Remove selection" />
        </StackLayout>
        <ScrollView x:Name="scrollArrows" MinimumHeightRequest="60" HeightRequest="60" BackgroundColor="AliceBlue">
            <ctl:ArrowUniformGrid AutomationId="scoreList" x:Name="scoreList"  Grid.Column="0" CountByRow="6" />
        </ScrollView>

        <ctl:CounterCommands x:Name="counterCommands" Counter="{Binding Counter}" />
    </StackLayout>
</ContentPage>
