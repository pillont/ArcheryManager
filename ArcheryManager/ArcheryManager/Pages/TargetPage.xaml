<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ctl="clr-namespace:ArcheryManager.CustomControls"
             xmlns:controls="clr-namespace:XFShapeView;assembly=XFShapeView"
             xmlns:shape="clr-namespace:XFShapeView;assembly=XFShapeView"
             x:Class="ArcheryManager.Pages.TargetPage">

    <Grid x:Name="globalGrid">

        <ctl:TotalCounter x:Name="totalCounter" Grid.Row="0" />
        <ctl:Target x:Name="customTarget"  MinimumHeightRequest="1200" MinimumWidthRequest="1200" />

        <ScrollView x:Name="scrollArrows" MinimumHeightRequest="60"  BackgroundColor="AliceBlue">
            <ctl:ArrowUniformGrid AutomationId="scoreList" x:Name="scoreList"   CountByRow="6" />
        </ScrollView>
    </Grid>
</ContentPage>
