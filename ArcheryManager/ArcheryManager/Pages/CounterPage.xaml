<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ctl="clr-namespace:ArcheryManager.CustomControls"
             x:Class="ArcheryManager.Pages.CounterPage">

    <StackLayout>
        <ctl:TotalCounter x:Name="totalCounter" />
        <ScrollView x:Name="scrollArrows" MinimumHeightRequest="60" HeightRequest="60" BackgroundColor="AliceBlue">
            <ctl:ArrowUniformGrid x:Name="scoreList" AutomationId="scoreList" Grid.Column="0" CountByRow="6" />
        </ScrollView>
        <ctl:CounterButtons x:Name="counterButtons" />
    </StackLayout>
</ContentPage>
