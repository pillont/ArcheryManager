<?xml version="1.0" encoding="utf-8" ?>
<page:ContentPageWithGeneralEvent xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:page="clr-namespace:ArcheryManager.Pages.PagesTemplates"
             xmlns:ctl="clr-namespace:ArcheryManager.CustomControls"
             x:Class="ArcheryManager.Pages.CounterButtonPage">
    <Grid x:Name="globalGrid">
        <ctl:TotalCounter x:Name="totalCounter" Grid.Row="0" BindingContext="{Binding Counter}" />
        <ScrollView x:Name="scrollArrows" BackgroundColor="AliceBlue" Grid.Row="1" />
        <ctl:CounterButtons x:Name="counterButtons" Grid.Row="2" BindingContext="{Binding CounterButtonsViewModel}" />
    </Grid>
</page:ContentPageWithGeneralEvent>
