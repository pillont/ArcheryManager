<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ArcheryManager.CustomControls.CounterCommands">
    <ContentView.Content>
        <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
            <Button AutomationId="removeArrow" Text="Remove last" Clicked="Button_RemoveLast" />
            <Button AutomationId="removeAllArrows" Text="Remove all"  Clicked="Button_RemoveAll" />
            <Button AutomationId="nextFlight" Text="New flight"  Clicked="Button_NewFlight" />
        </StackLayout>
    </ContentView.Content>
</ContentView>
