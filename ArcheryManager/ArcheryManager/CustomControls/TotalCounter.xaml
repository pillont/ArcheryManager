<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ArcheryManager.CustomControls.TotalCounter">
    <ContentView.Content>
        <Grid HeightRequest="100">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackLayout HorizontalOptions="Center" Grid.Column="0">
                <Label Text="Flight :" />
                <Label FontAttributes="Bold" TextColor="Black" HorizontalTextAlignment="Center" Text="{Binding Flight}" />
            </StackLayout>
            <StackLayout Grid.Column="1" HorizontalOptions="Center">
                <Label Text="Total :" />
                <Label FontAttributes="Bold" TextColor="Black" HorizontalTextAlignment="Center" Text="{Binding Total}" />
            </StackLayout>
        </Grid>
    </ContentView.Content>
</ContentView>
